<nav class="menu" role="nav">
    <ul>
        {{#foreach navigation}}
                <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}"><a href="{{url absolute="true"}}">{{author}}</a></li>
        {{/foreach}}
    </ul>
</nav>
