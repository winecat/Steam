<nav class="menu" role="nav">
    <ul>
        {{#foreach navigation}}
                <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}"><a href="{{{{author}} absolute="true"}}">{{author}}</a></li>
        {{/foreach}}
    </ul>
</nav>
